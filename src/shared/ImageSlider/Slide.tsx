import React, { FunctionComponent, ReactElement } from 'react';
import { SlideProps } from './interfaces';

const Slide: FunctionComponent<SlideProps> = (props: SlideProps): ReactElement => {
  return <>A</>;
};

export default Slide;
